{
  "name": "crowdfunding-dapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Smart contracts del crowdfunding descentralizado (capa Hardhat/Solidity). El frontend vive en /frontend con su propio package.json.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:gas": "REPORT_GAS=true hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "hardhat": "3.9.1",
    "@nomicfoundation/hardhat-toolbox-viem": "5.0.5",
    "@openzeppelin/contracts": "5.6.1",
    "viem": "2.54.1",
    "typescript": "6.0.3"
  }
}
